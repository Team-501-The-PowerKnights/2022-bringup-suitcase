name: build

on:
  - push
  - pull_request

jobs:
  gradle:
    runs-on: ubuntu-latest
    container: wpilib/roborio-cross-ubuntu:2022-18.04
    steps:
      - uses: actions/checkout@v2
      - run: |
          chmod +x gradlew
          ./gradlew build
